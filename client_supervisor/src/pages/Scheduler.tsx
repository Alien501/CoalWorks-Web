const Scheduler = () => {
    return(
        <div id="Scheduler-wrapper" className="flex items-center justify-center">
            <h1>Scheduler</h1>
        </div>
    )
}

export default Scheduler;